# secure-login-system
A project developed in Python that simulates a secure authentication system. It includes user login verification, password validation, and a basic mechanism for detecting repeated login attempts (brute-force attack protection).
